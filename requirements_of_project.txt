README2:

installing various needed libraries in Jupyter notebook:

paste the following code:

!pip install numpy
!pip install pandas
!pip install scipy
!pip install opencv-python
!pip install matplotlib


after installation try to run following commands:
 
import numpy as np
import pandas as pd
import scipy as sp
import CV2 as cv
import matplotlib.pyplot as plt

##################################################
These command should run without error:
NOTE:These commands have to be run in Jupyter notebook and make sure to install python 3. and pip beforehand
